name: Publish Extension

on:
  release:
    types:
      - created

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: cursor-acp-extension/package-lock.json

      - name: Install dependencies
        working-directory: cursor-acp-extension
        run: npm ci

      - name: Package Extension
        working-directory: cursor-acp-extension
        run: npm run package

      - name: Get Extension Filename
        id: get_filename
        working-directory: cursor-acp-extension
        run: echo "filename=$(ls *.vsix)" >> $GITHUB_OUTPUT

      - name: Upload Extension to Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload ${{ github.event.release.tag_name }} ./cursor-acp-extension/${{ steps.get_filename.outputs.filename }} --clobber

      - name: Publish Extension to Open VSX
        if: ${{ vars.PUBLISH_TO_OVSX == 'true' }}
        working-directory: cursor-acp-extension
        env:
          OVSX_PAT: ${{ secrets.OVSX_PAT }}
        run: |
          npx ovsx publish ./${{ steps.get_filename.outputs.filename }} -p ${{ secrets.OVSX_PAT }}
